entities:
  - name: ProductItem
    aggregateRoot: false
    fields:
      - name:String
      - quantity:Integer
      - position:Integer

  - name: Product
    aggregateRoot: true
    fields:
      - name:String
      - price:BigDecimal
    relations:
      - productItems:ManyToOne:ProductItem
